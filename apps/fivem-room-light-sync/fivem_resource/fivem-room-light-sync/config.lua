-- This template is replaced when you use “Export FiveM resource” in the desktop app.
-- Do not publish the generated token or expose this endpoint outside your own PC.
Config = {}
Config.Endpoint = 'http://127.0.0.1:17831/sync'
Config.Token = 'replace-by-exporting-from-the-desktop-app'
Config.PollIntervalMs = 250
